# Backend environment. Copy to .env.local and fill locally; never commit real values.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Verified account allowed to administer bookings/concerts.
# Empty means no account has admin access. Server only.
ADMIN_EMAIL=
